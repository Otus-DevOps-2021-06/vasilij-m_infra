db_host: "{{ hostvars['reddit-db']['ansible_eth0']['ipv4']['address'] }}"
deploy_user: appuser
nginx_sites:
  default:
    - listen 80
    - server_name "reddit"
    - location / {
        proxy_pass http://127.0.0.1:9292;
      }
